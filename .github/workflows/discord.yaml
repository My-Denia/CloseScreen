name: PR notification disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notify-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: External notification disabled for fork takeover
        run: |
          echo "External chat notifications are disabled for this fork takeover."
          echo "No webhook or forum posting is performed by this workflow."
