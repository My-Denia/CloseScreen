name: Bump Nix package on release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag that would bump the Nix package"
        required: true
        type: string

permissions:
  contents: read

jobs:
  bump-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Nix package bump disabled for fork takeover
        run: |
          echo "TODO: Re-enable after the maintainer confirms the Nix package release workflow policy."
          echo "No branch, commit, push, or pull request is created by this workflow."
